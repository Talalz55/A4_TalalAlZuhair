CREATE TABLE book (
                      id BIGINT PRIMARY KEY AUTO_INCREMENT,
                      title VARCHAR(255),
                      author_name VARCHAR(255),
                      price DOUBLE,
                      quantity INT
);
